<head>
    <link rel="stylesheet/less" type="text/css" href="styles/importer.less" />
    <script src="js/less.js" type="text/javascript"></script>
</head>

<h1>Actors</h1>
<ul>
    <% for(let i in actors ) {
        let actor = actors[i];
    %>
    <li><a href='doc/actor/<%= actor.shortname %>'><%= actor.name %></a> - <%= actor.description %></li>
    <% } %>
</ul>
<% for(let i in actors) {
    let actor = actors[i];
%>

<img src="./Actors.puml" >

<h2><%= actor.name %></h1>
<p><%= actor.description %></p>
<h3>Use Cases</h2>
<ul>
<% for(let uname in actor.usecases) {
    let ucname = actor.usecases[uname].prefix + '/' + uname;
    let name = actor.usecases[uname].name
-%>
    <li><a href='doc/usecase/<%= ucname %>'><%= name %></a></li>
<% } %>
</ul>
<% } %>
