// const renderer = require('../../src/Documentation/Renderer.js');

module.exports = {
    friendlyName: 'isStale',
    description: 'Check if the snapshot is stale',
    inputs: {
    },

    fn: function (obj, inputs, env) {
        return false;
    }
};