// const renderer = require('../../src/Documentation/Renderer.js');

module.exports = {
    friendlyName: 'resolve',
    description: 'Resolve the snapshot information from the microservice',
    inputs: {
    },

    fn: function (obj, inputs, env) {
        return true;
    }
};