// const renderer = require('../../src/Documentation/Renderer.js');

module.exports = {
    friendlyName: 'toJSON',
    description: 'Returns the object as a JSON object',
    inputs: {
    },

    fn: function (obj, inputs, env) {
        return obj;
    }
};