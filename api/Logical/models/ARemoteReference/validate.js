// const renderer = require('../../src/Documentation/Renderer.js');

module.exports = {
    friendlyName: 'validate',
    description: 'Validate the object on the microservice',
    inputs: {
    },

    fn: function (obj, inputs, env) {
        return true;
    }
};